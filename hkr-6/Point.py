class Point:
    import math
    def __init__(self,x,y):
        sefl.y = x
        self.y = y
    def move(self,x,y):
        sefl.y = x
        self.y = y
    def reset(slef):
        sefl.y = 0
        self.y = 0
    def calculate_distance(self, point):
        d = 0.0
        return d = math.sqrt(math.pow((self.x - point.x),2) + math.pow((self.y - point.y),2))
        
