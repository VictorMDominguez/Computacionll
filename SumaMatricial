
import  sys
arr = []
arr = map(int,raw_input().strip().split(' '))
arr.append(arr_temp)
n = len(arr)
for i in xrange(n):
        lista =  map(int,raw_input().strip().split(' '))
        for j in xrange(n):
                arr[i] += lista[j]
print arr
